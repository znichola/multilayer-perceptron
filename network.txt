# This is interpreted as a python script with some things pre-imported:
# 'input_shape' and 'output_shape' are derived from the data, and
# all mlp classes (Sequential, Dense, Sigmoid, SoftMax, ...)
# It's easy the write an invalid config, so don't be dumb.

model = Sequential([
    Dense(input_shape, 24, activation=Sigmoid, weight_init=HeUniform),
    Dense(24, 24, activation=Sigmoid, weight_init=HeUniform),
    Dense(24, 24, activation=Sigmoid, weight_init=HeUniform),
    Dense(24, output_shape, activation=SoftMax, weight_init=HeUniform),
])

# CategoricalCrossentropy
model.compile(loss=CategoricalCrossentropy, lr=0.1, epochs=200, batch=80, patience=0)
# model.compile(loss=CategoricalCrossentropy, lr=0.0134, epochs=400, batch=50, patience=0)
