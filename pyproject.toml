[project]
name = "multilayer-perceptron"
version = "0.1.0"
description = "An introduction to artificial neural networks"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.10.6",
    "pandas>=2.3.2",
]
